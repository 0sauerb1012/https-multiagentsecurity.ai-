# Shared Taxonomy

These JSON files define the initial shared taxonomy for `multiagentsecurity.ai`.

They are intended to be:

- Human-readable
- Version-controlled
- Shared between the web app, ingestion pipeline, and database seed logic

The database seed file currently mirrors these values manually. A future improvement is to generate seed SQL from this package to reduce drift.
